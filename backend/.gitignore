# Compiled Java class files
*.class

# Maven
target/
.mvn/timing.properties

# Environment & Secrets - NUNCA subir al repositorio
.env
.env.*
*.env
# OJO: application-local.properties NO se ignora — es la config del PERFIL `local`
# (el cerebro embebido del instalable, ADR-009): solo dummies y defaults, cero
# secretos. Los secretos reales siguen viniendo de variables de entorno.

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log

# Debug scripts sueltos en la RAÍZ de backend/ (nunca deben estar en el repo).
# Anclados con '/' a la raíz: sin esto atrapaban clases legítimas de src/ como
# UpdateRollout.java / UpdatesController.java (bug que dejó un commit incompleto).
/Check*.java
/Test*.java
/Update*.java
scripts/
