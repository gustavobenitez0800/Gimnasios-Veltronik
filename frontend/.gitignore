# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Build de Electron y lockfile espurio (el canónico es pnpm-lock.yaml)
release
release-builds
dist_electron
package-lock.json

# El cerebro embebido (JRE jlink + jar, ~200MB): artefacto de build, lo produce
# build-tools/build-backend.ps1 — jamás se commitea
resources/backend


# Environment & Secrets - NUNCA subir al repositorio
.env
.env.production
.env.staging

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
